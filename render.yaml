services:
  - type: web
    name: nonotalk-backend
    env: python
    rootDirectory: nonotalk-backend
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: gunicorn src.main:app --bind 0.0.0.0:$PORT
    healthCheckPath: /api/health
    autoDeploy: true
